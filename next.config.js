/** @type {import('next').NextConfig} */
const nextConfig = {
    // Might not need to use these
    // typescript: {
    //     ignoreBuildErrors: true,
    // },
    // esLint: {
    //     ignoreDuringBuilds: true,
    // }
}

module.exports = nextConfig
